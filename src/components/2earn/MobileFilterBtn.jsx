import styled from "styled-components";

const MobileFilterBtn = () => {
  return <MFWrapper>MobileFilterBtn</MFWrapper>;
};

export default MobileFilterBtn;

export const MFWrapper = styled.div`
  width: 20%;
`;
